#include "main.h"
/**
 * _abs - computes the absolute of an integer
 * @n: the integer
 * return: the absolute value of the integer
 */
int _abs(int)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
