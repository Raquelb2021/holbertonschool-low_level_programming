/**
 * main - entry point
 * Return: 0
 */

#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase.
 * @letter 
 */
 *void print_alphabet(void)
{
		char letter;

		for (letter = 'a'; letter <= 'z'; letter++)
		_putchar(letter);

		_putchar('\n');
}
