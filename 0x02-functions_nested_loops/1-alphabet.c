#include "main.h"
/**
 * main - entry point
 * Return: 0
 */
int main(void)
{
	void print_alphabet(void);

		char ch;

		for (ch = 'a'; ch <= 'z'; ch++)
			_putchar(ch);

		_putchar('\n');
		return (0);
}
