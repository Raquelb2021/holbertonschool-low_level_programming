#include "main.h"

/**
 * print_alphabet - print alphabet in lowercase 10 times
 *
 * return:0(success)
 */
	void print_alphabet_x10(void);
{
	char ch;
	int count = 10;

	for(ch = 'a'; ch <= 'z'; ch++)
		_putchar(ch);

		_putchar('\n')
}
