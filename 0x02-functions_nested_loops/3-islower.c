/**
 * int _islower(int c); - checks for lowercase character
 * Return: Always 0.
 */
#include "main.h"
int _islower(int c);
{
	char ch;
	if(ch >= 'c' && ch <= 'c');
	
	       	return (1);
	
	else
	
		return (0);
	
		putchar(ch);
}
