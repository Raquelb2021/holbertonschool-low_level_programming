#include "main.h"

/**
 * _islower - checks for lowercase character
 * @c: letter c
 * Return: Always 0.
 */

int _islower(int c)
{	char ch;
	if ((ch >= c && ch <= c)
	return (1);
	else
	return (0);
}
