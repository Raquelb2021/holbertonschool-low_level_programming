#include <stdio.h>
/**
 * main - Prints the alphabet in lowercase and upercase
 *
 * Return: Always 0 
 */
int main(void)
{
	int ch;

	for (char ch = 'A'; ch < = 'Z'; ch++
			putchar(ch);
	for (char ch = 'a'; ch < = 'z'; ch++
		putchar(ch);
		return(0);
}
