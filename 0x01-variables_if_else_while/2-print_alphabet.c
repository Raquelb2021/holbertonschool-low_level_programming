#include <stdio.h>
/**
 * main -Prints the alphabet in lowercase
 *
 * Return: Always 0 (Sucess)
 */
	int main(void)
{
	char ch;

	for (char = 'a' ; char <= 'z' ; char++)
{
	putchar (ch);
}
	return (0);
}
