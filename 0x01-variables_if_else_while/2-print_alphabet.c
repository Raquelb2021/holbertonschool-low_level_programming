#include <stdio.h>
/**
 * main -Prints the alphabet in lowercase
 *
 * Return: Always 0 (Sucess)
 */
	int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ; ch++"\n")
{
	putchar (ch);
}
	return (0);
}
