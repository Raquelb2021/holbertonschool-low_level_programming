#include <stdlib.h>

/**
 * main -Prints the alphabet in lowercase
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	int letter;
	for(letter='a';ch<='z';letter++)
	putchar(letter);
	printf("\n");

	return(0);
}
