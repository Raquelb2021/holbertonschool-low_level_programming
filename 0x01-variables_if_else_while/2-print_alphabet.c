#include <stdio.h>
/**
 * main -Entry pint
 *
 * Return: Always 0 (Sucess)
 */
int main(void)
{
	char ch;

	printf("\nLowercase characers : \n");
	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch);
		putchar ('\n') ;
		getch();
}
