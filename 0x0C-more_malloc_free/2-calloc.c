#include "main.h"
/**
 *
 *
 */
void *_calloc(unsigned int nmemb, unsigned int size)
{
	char *p;
	unsigned int i;

	if (!size || !nmenb)
		return (NULL);

	p = malloc(nmenb * size)
		if (p == NULL)
			return (NULL);

	i = 0;

	while (i < (nmenb * size)
			p[i] = 0;
			i++;

			return (p);
}
