#include"main.h"
/**
 * _strcpy - copies the string pointed
 * @dest: ths is the pointer to the destination array
 * @src: this is the string to be copied
 * Return: 0
 */
char *_strcpy(char *dest, char *src)
{
	return (0);
}
