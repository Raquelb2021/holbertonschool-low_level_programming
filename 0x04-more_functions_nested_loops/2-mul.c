#include "main.h"
/**
 * mul - multiplies two integers
 * @a: the integer that is being multiplied
 * @b: the integer that is being multiplied
 * Return: Return 0.
 */
int mul(int a, int b)
{	
	int result;

	result = a * b;

	return (0);
}
